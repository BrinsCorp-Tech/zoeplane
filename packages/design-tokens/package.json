{
  "name": "@zoeplane/design-tokens",
  "version": "0.1.0",
  "description": "ZoePlane design tokens — DTCG JSON source → Style Dictionary build pipeline",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "node style-dictionary.config.mjs",
    "check-tokens": "node style-dictionary.config.mjs && echo 'tokens:check PASS — src/styles/tokens.css generated successfully from seed JSON'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
