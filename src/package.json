{
  "name": "@zoeplane/ui",
  "version": "0.1.0",
  "description": "ZoePlane React UI — Vite + React 18 + TypeScript 5.5 + Tailwind CSS 4",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "bun run --cwd .. tokens:build",
    "dev": "vite",
    "prebuild": "bun run --cwd .. tokens:build",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "1.1.15",
    "@radix-ui/react-dropdown-menu": "2.1.16",
    "@radix-ui/react-slot": "1.2.4",
    "@radix-ui/react-tooltip": "1.2.8",
    "@zoeplane/shared-types": "workspace:*",
    "next-themes": "0.4.6",
    "sonner": "2.0.7"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
